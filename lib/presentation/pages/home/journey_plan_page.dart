import 'package:flutter/material.dart';

class JourneyPlanPage extends StatelessWidget {
  const JourneyPlanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Journey Plan'),
      ),
      body: Center(
        child: Text('Journey Plan Page'),
      ),
    );
  }
}
